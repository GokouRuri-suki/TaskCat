"""
音频处理模块（已弃用）

当前架构设计中，音频播放完全由前端（Java 应用）负责。
后端只通过 API 响应返回音频文件 URL（如 "/sounds/notification.wav"）。

前端处理流程：
1. 调用后端 API（创建任务、同步等）
2. 后端响应中包含 "sound" 字段，指向音频文件 URL
3. 前端下载音频文件
4. 前端使用 Java/JavaFX 播放音频

这样设计的好处：
- 后端无需依赖系统音频命令
- 不阻塞 API 响应
- 更可靠（前端有音频硬件）
- 跨平台支持更好
"""
